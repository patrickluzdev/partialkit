---
title: Button
description: Displays a button or a component that looks like a button.
---

import Preview from "../../../components/Preview.astro";

<Preview src="button/overview" layout="row" />

## Variants

| Class | Use |
| --- | --- |
| `.btn` | Primary action. The base class already carries the default variant |
| `.btn-secondary` | Secondary action |
| `.btn-outline` | Bordered, transparent background |
| `.btn-ghost` | No border, no background until hovered |
| `.btn-destructive` | Irreversible action |
| `.btn-link` | Looks like a link, behaves like a button |

## Sizes

<Preview src="button/sizes" layout="row" />

`.btn-icon` makes the button square. Combine it with `.btn-sm` or `.btn-lg` for the smaller and
larger squares. An icon-only button needs an `aria-label` or `.sr-only` text.

## States

<Preview src="button/states" layout="row" />

Any element takes the classes, but reach for a real `<button>` or `<a>` so keyboard activation,
focus order and `disabled` come from the platform.

## With an icon

An `svg` child is sized and made non-interactive automatically.

<Preview src="button/with-an-icon" />
