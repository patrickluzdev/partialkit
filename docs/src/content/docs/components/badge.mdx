---
title: Badge
description: Displays a badge or a component that looks like a badge.
---

import Preview from "../../../components/Preview.astro";

<Preview src="badge/overview" layout="row" />

| Class | Use |
| --- | --- |
| `.badge` | Default, filled with the primary colour |
| `.badge-secondary` | Lower emphasis |
| `.badge-destructive` | Errors and danger states |
| `.badge-outline` | Border only |
| `.badge-ghost` | No surface until hovered |
| `.badge-link` | Reads as a link; put it on an `<a>` |

Only `.badge-ghost` and `.badge-link` react to the pointer on their own. The filled variants take a
hover surface when the badge is an `<a>`, since a plain `<span>` is not something you can activate.

Give an icon `data-icon="inline-start"` or `data-icon="inline-end"` to tighten its spacing on that
side.

## With an icon

<Preview src="badge/with-an-icon" layout="row" />

A badge is decorative markup. When it carries state a user must know about, put that state in text
as well, or give the badge an accessible name.
