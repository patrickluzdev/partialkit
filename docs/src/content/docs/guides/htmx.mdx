---
title: htmx and Go
description: How partialkit survives swaps, and what the markup looks like inside Go templates.
---

## Why swapped markup just works

Two mechanisms, chosen so that nothing has to be re-initialised after a response lands:

- **Triggers use event delegation on `document`.** A `data-pk-dialog-open` button works the moment
  it exists, whether it shipped with the page or arrived in a swap.
- **Anything needing per-element state is mounted by a `MutationObserver`.** Dialogs and dropdown
  menus get their listeners and ARIA the instant they enter the DOM, and their cleanups run when
  they leave.

There is no `htmx.process()` equivalent to call, and no `htmx:afterSwap` handler to write.

```html
<button class="btn" hx-get="/projects/new" hx-target="#panel">New project</button>

<div id="panel">
  <!-- A dialog or menu in this response is live as soon as it lands. -->
</div>
```

## Go templates

```html
{{ define "confirm-delete" }}
  <button class="btn btn-destructive" data-pk-dialog-open="delete-{{ .ID }}">Delete</button>

  <dialog class="dialog" id="delete-{{ .ID }}">
    <div class="dialog-header">
      <h2 class="dialog-title">Delete {{ .Name }}</h2>
      <p class="dialog-description">This action cannot be undone.</p>
    </div>
    <div class="dialog-footer">
      <button class="btn btn-outline" data-pk-dialog-close>Cancel</button>
      <button class="btn btn-destructive"
              hx-delete="/projects/{{ .ID }}"
              hx-target="#project-list">
        Delete
      </button>
    </div>
  </dialog>
{{ end }}
```

## Server-side validation

Fields style their error state from `aria-invalid`, so a re-render needs no extra class and no
client-side state.

```html
<div class="field">
  <label class="label" for="handle">Handle</label>
  <input class="input" id="handle" name="handle" value="{{ .Handle }}"
         {{ if .Error }}aria-invalid="true"{{ end }} />
  {{ if .Error }}<p class="field-error">{{ .Error }}</p>{{ end }}
</div>
```

## Closing a dialog after a successful request

`data-pk-dialog-close` covers the plain case. When the server decides, close it from the htmx event:

```html
<dialog class="dialog" id="new-project"
        hx-on::after-request="if (event.detail.successful) this.close()">
  ...
</dialog>
```

## templ

The classes are just strings, so templ components work the same way:

```go
templ ConfirmDelete(project Project) {
	<button class="btn btn-destructive" data-pk-dialog-open={ "delete-" + project.ID }>
		Delete
	</button>
	<dialog class="dialog" id={ "delete-" + project.ID }>
		<div class="dialog-header">
			<h2 class="dialog-title">Delete { project.Name }</h2>
		</div>
	</dialog>
}
```
